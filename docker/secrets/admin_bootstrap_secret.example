# Replace this content with a strong, unique bootstrap token before production use.
# Mount this file as a Docker secret and point ADMIN_BOOTSTRAP_SECRET_FILE to /run/secrets/admin_bootstrap_secret.
change-this-bootstrap-token
